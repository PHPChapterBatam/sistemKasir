import React, { Component } from 'react'
import { connect } from "react-redux";
import { TextFieldForm } from "../../utils/FormComponent";

 class TransaksiEdit extends Component {
  render() {
    return (
      
    )
  }
}


export default connect()(TransaksiEdit);